run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - gosimple
    - staticcheck
    - unused
    - govet
    - revive
    - gofmt
    - misspell
    - unparam

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0
